# DraftPeek Server Dependencies (PINNED — see ../docs/reports/协作同步服务设计评估报告_v1.0.30.md P0-1)
# Install: pip install -r server/requirements.txt
#
# Security note: all versions are exact-pinned (==) to make the dependency
# tree reproducible and to let `pip-audit` / dependency-review catch drift.
#
# IMPORTANT: websockets is pinned to 13.1 on purpose — server/crdt_server.py
# uses the legacy `websockets.serve` / `WebSocketServerProtocol` API that was
# removed in websockets >= 15. Do not bump without migrating the server code
# to the `websockets.asyncio` API.

# ── CRDT Collaboration Server (server/crdt_server.py) ──────────────────────
# pycrdt is the Python binding for Yjs CRDT (formerly ypy)
websockets==17.1
uvicorn==0.52.4
pycrdt==0.14.4

# ── Cloud Sync Server (server/sync_server/main.py) ─────────────────────────
fastapi==0.141.1
python-multipart==0.0.32
aiosqlite==0.22.1

# ── Play Integrity Server (server/integrity_server.py) ────────────────────
# httpx is a hard runtime dependency of integrity_server.py (calls Google API)
httpx==0.28.1

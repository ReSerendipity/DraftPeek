# DraftPeek gitleaks 配置
# 基于官方默认规则扩展（useDefault = true 保留全部内置检测规则），
# 仅追加经人工核验的误报 allowlist，保证 CI 门禁可用且不误伤。

[extend]
useDefault = true

[allowlist]
description = "经人工核验的统一误报清单（诱饵安全类：刻意低熵占位常量，见 DecoyCryptoClasses.kt）"
paths = ['''DecoyCryptoClasses\.kt''']